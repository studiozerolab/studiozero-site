---
const items = [
  {
    title: "Web Design",
    body: "Simple, modern sites that look great on phones.",
    icon: "browser",
  },
  {
    title: "Site Redesigns",
    body: "Refresh your look without starting from scratch.",
    icon: "refresh",
  },
  {
    title: "Consulting & Support",
    body: "Advice, tweaks, maintenance—only what you need.",
    icon: "wrench",
  },
];
---
<!-- section-heading   -  text-2xl sm:text-3xl font-semibold text-zinc-900 mb-6 -->
<section id="services" class="bg-zinc-50">
  <div class="wrap py-16">
    <h2 class="section-header pop">Services</h2>

    <div class="grid-soft rounded-xl p-6 sm:p-8">
      <div class="grid gap-6 sm:grid-cols-3">
        {items.map(({ title, body, icon }) => (
          <article class="rounded-lg bg-white border border-zinc-200 p-6 hover:shadow-md transition-shadow">
            <div class="text-zinc-800 mb-3">
              {
                icon === "browser" && (
                  <svg viewBox="0 0 24 24" class="h-6 w-6">
                    <rect x="3" y="5" width="18" height="14" rx="2" class="fill-none stroke-current" />
                    <circle cx="7" cy="9" r="1" class="fill-current" />
                    <circle cx="10" cy="9" r="1" class="fill-current" />
                    <rect x="3" y="7" width="18" height="2" class="fill-current opacity-20" />
                  </svg>
                )
              }
              {
                icon === "refresh" && (
                  <svg viewBox="0 0 24 24" class="h-6 w-6">
                    <path d="M4 4v6h6" class="fill-none stroke-current" />
                    <path d="M20 20v-6h-6" class="fill-none stroke-current" />
                    <path d="M20 10a8 8 0 0 0-14-4" class="fill-none stroke-current" />
                    <path d="M4 14a8 8 0 0 0 14 4" class="fill-none stroke-current" />
                  </svg>
                )
              }
              {
                icon === "wrench" && (
                  <svg viewBox="0 0 24 24" class="h-6 w-6">
                    <path d="M14.5 2l1 2.5 2.5 1-2.5 1-1 2.5-1-2.5-2.5-1 2.5-1 1-2.5z" class="fill-none stroke-current" />
                    <circle cx="12" cy="14" r="4" class="fill-none stroke-current" />
                    <path d="M12 10v-3M12 18v3M8 14H5M19 14h-3" class="fill-none stroke-current" />
                  </svg>
                )
              }
            </div>

            <h3 class="font-semibold text-zinc-900 mb-1">{title}</h3>
            <p class="text-sm text-zinc-600">{body}</p>
          </article>
        ))}
      </div>
    </div>
  </div>
</section>
